/*
 *  Sends the test parameter structure and number of tests
 */
void VXLIB_tableLookup_getTestParams (
    VXLIB_tableLookup_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (VXLIB_tableLookup_testParams_t);
}
