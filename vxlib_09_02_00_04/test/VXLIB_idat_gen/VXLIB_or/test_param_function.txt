/*
 *  Sends the test parameter structure and number of tests
 */
void VXLIB_or_getTestParams (
    VXLIB_or_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (VXLIB_or_testParams_t);
}
