/*
 *  Sends the test parameter structure and number of tests
 */
void VXLIB_addSquare_getTestParams (
    VXLIB_addSquare_testParams_t **params,
    int32_t *                                 numTests)
{
   *params = testParams;
   *numTests =
       sizeof (testParams) / sizeof (VXLIB_addSquare_testParams_t);
}
