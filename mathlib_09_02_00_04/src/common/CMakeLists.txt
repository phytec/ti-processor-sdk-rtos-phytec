# file(GLOB_RECURSE COMMON_FILES "*.cpp" "*.h")
# # message("common files are: ${COMMON_FILES}")

# add_library(MATHLIB_common STATIC ${COMMON_FILES})

# MATHLIB_set_lib_extension(MATHLIB_common)

# target_include_directories (MATHLIB_common PUBLIC ../../src/common . ${C7x_INCLUDES})
